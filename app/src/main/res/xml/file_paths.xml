<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Mapeamento existente para o cache (se você o usa para compartilhamento temporário ) -->
    <cache-path
        name="shared_pdfs"
        path="." />

    <!-- NOVO: Mapeamento para o diretório 'reports' criado pelo ReportsViewModel -->
    <files-path
        name="reports"
        path="reports/" />
</paths>
